# CLAUDE.md — REINS 新着・更新物件 自動ダウンロードツール

このファイルは Claude Code への引き継ぎ用ドキュメントです。プロジェクトの目的・構成・
これまでにリバースエンジニアリングした REINS の画面仕様・現状・今後の課題をまとめています。

---

## 1. 目的

REINS IP（不動産流通標準情報システム / https://system.reins.jp ）にログインし、
**ワンタッチ検索に保存された「自動：」で始まる検索条件**を毎日自動で実行して、
**新着・更新物件の一覧（CSV/Excel）**を保存する。PDF図面取得は任意（課金）。
Mac(launchd)/Windows(タスクスケジューラ)で毎朝8:00に自動実行する想定。

会員アカウント：（株）鎌倉鑑定。ユーザーは非エンジニア。

---

## 2. 実行環境・依存

- Python 3.10+（ユーザー環境は 3.13、Mac）
- 依存：`playwright>=1.40`, `PyYAML>=6.0`, `openpyxl>=3.1`（requirements.txt）
- ブラウザ：`playwright install chromium` 済み
- 作業フォルダ（ユーザー実機）：`~/Documents/ai/reins-downloader`

### 実行コマンド
```bash
python3 reins_downloader.py              # 通常実行（毎日）
python3 reins_downloader.py --headed      # ブラウザ表示で実行（デバッグ）
python3 reins_downloader.py --list-only   # 「自動：」保存条件の検出だけ確認
```

---

## 3. ファイル構成

- `reins_downloader.py` … 本体（Playwright sync API）
- `config.example.yaml` / `config.yaml` … 設定（認証情報・出力・PDF・selectors）
- `install_schedule_mac.command` / `install_schedule_windows.bat` … 毎日8:00の自動実行登録（ダブルクリック/管理者実行）
- `uninstall_schedule_*` … 解除
- `setup_mac.command` / `setup_windows.bat` … 依存インストール
- `run.command` / `run.bat` … 手動実行
- `scheduler/` … 旧・手動スケジュール手順（フォールバック）
- `downloads/YYYY-MM-DD/新着更新物件.csv|.xlsx` … 出力
- `state/downloaded.json` … 重複・更新判定の状態
- `logs/` … 実行ログ、`error_screenshot.png` … エラー時の画面

---

## 4. 動作フロー（実画面で確認済み）

1. **ログイン**（`Reins.login`）
   - `login/main/KG/GKG001200` を開く（SPA、描画待ち必須）
   - ユーザID＝`input[type='text']`, パスワード＝`input[type='password']` に fill
   - **「所属機構の規程及びガイドラインを遵守します」チェックボックスが必須**。
     未チェックだと「チェックを入れてログインしてください」で弾かれる。
     `_ensure_agree_checked()` が get_by_label→ラベルテキストclick→全checkbox総当たりの順で確実にON。
   - `ログイン` ボタン（`button:has-text('ログイン')`, type=button）をクリック
   - 成功時 URL は `main/KG/GKG003100`（メインメニュー）
   - 認証失敗時モーダル「ユーザIDもしくはパスワードが誤っています」。**連続失敗でアカウントロックの恐れ**があるため要注意。

2. **検索画面**（`open_search`）
   - `main/BK/GBK001210`（売買検索条件入力）へ goto
   - ワンタッチ検索が畳まれていれば「検索条件を表示」をクリックで展開

3. **保存条件ループ**（`list_saved_searches` / `run_saved_search`）
   - ワンタッチ検索の `<select>`（`saved_search_select()` が「自動」を含むoptionを持つselectを特定）
   - option ラベルが `自動：` で始まるものだけを対象（ユーザー環境で17〜18件）
   - 各条件：`ElementHandle.select_option(label=...)` → `読込` クリック →
     **「検索条件を読込みました。」モーダルのOKを閉じる**（`_dismiss_dialog(wait_appear=True)`。
     ※閉じる前に検索を押すと `modal ... intercepts pointer events` で失敗する。要 wait）→
     `検索`（exact=True）クリック → 結果一覧 `main/BK/GBK002100` へ

4. **結果スクレイピング**（`scrape_all_pages` / `parse_records` / `parse_block`）
   - 結果は**種別タブ**（例「売外全(80件)」「売外一(9件)」）に分かれる。role=tab を順にクリック
   - **ページ送り**：`menuitem` "Go to next page"（`_goto_next_page`）。50件/ページ
   - 一覧は `body.inner_text()` を取得し、**12桁の物件番号**を区切りに1物件ずつ分割してパース
   - 抽出項目：物件番号/種目/取引態様/取引状況/オーナーチェンジ/価格/用途地域/土地面積/建物面積/所在地/建物名/沿線駅/交通/商号/築年月/電話番号

5. **新着・更新判定**
   - `state/downloaded.json` に `物件番号 -> 署名(価格|取引状況|オーナーチェンジ)` を保存
   - 署名が変わった／未知の物件番号だけを当日の出力に含める（＝新着＋更新）

6. **出力**：`write_csv`（UTF-8 BOM）/ `write_xlsx`（openpyxl）

7. **PDF図面**（任意・`pdf.download: true` の時のみ / `download_pdfs`）
   - 「ページ内全選択」→「図面一括取得」で download を待つ実装。**課金対象**。**未検証**（実行していない）。

---

## 5. 確認済みの画面詳細（selector/値）

- 物件種別1 `<select>` option: 売土地=01 / 売一戸建=02 / **売マンション=03** / 売外全=04 / 売外一=05
- 物件種目（マンション）: 新築マンション=01 / 中古マンション=02 / …
- 検索の必須条件：**物件種別1** と、**所在地1の都道府県 等の立地条件**（未入力だとエラーモーダル）
  → ただし本ツールは保存条件（ワンタッチ検索）を読み込むので個別入力は不要
- 新着・更新の絞り込み（在庫のみ「その他検索項目」）:
  **登録年月日** と **変更年月日** に「前日/当日/3日以内/1週間以内/1ヶ月以内/日付指定」ラジオ。
  「いずれかに一致」仕様なので、**登録=前日＋変更=前日**で新着・更新を表現できる。
  → 各「自動：」保存条件側にこれを設定しておくと、日次で当日分だけに絞れる。
- 結果一覧の列順（ヘッダ）：No / 物件番号 / 物件種目 / 土地面積 / 所在地 / 取引態様 / 価格 /
  用途地域 / 建物面積 / 建物名 / 取引状況 / 接道状況 / 沿線駅 / 交通 / 接道1 / 商号 / 築年月 / 電話番号
  ※「オーナーチェンジ」は取引態様の直後に挿入されることがある。建物名・築年月は無い物件がある（"-"表示 or 行自体が無い）。
  そのため parse_block は固定位置ではなく「2つ目の㎡＝建物面積、その次＝建物名、電話番号の直前＝商号（築年月ならさらに手前）」等のアンカーで抽出している。
- 画面は BootstrapVue（モーダルは `div.modal.fade.show` / OKは role=button name=OK）。

---

## 6. 現状（2026-06-24 時点）

- **エンドツーエンドで成功**。`--headed` 実行で18件の「自動：」条件を処理し、**1051件**を CSV/Excel 出力（code=0）。
- ログイン・遵守チェック・保存条件ループ・読込ダイアログ処理・タブ/ページ送り・パース・重複判定まで動作。
- 初回実行のため全ヒットを「新着」として保存（＝baseline）。次回以降は差分のみ。

---

## 7. 今後の課題 / 改善候補

1. **PDF図面（図面一括取得）の検証**：課金対象のため未実行。`pdf.download: true` で `download_pdfs` を
   `--headed` で挙動確認（課金確認画面が挟まる可能性、ダウンロード形式=zip/pdf、保存名、`max_per_search` の効き）。
2. **日付絞り込み運用**：各保存条件に「登録=前日/変更=前日」を入れる運用を案内 or ツール側で読込後に自動設定する拡張。
3. **パースの堅牢化**：2026年の東日本レインズ画面に合わせたヒューリスティック。画面変更時は
   `parse_block` と `selectors` を調整。可能なら結果行の DOM 構造（要素単位）からの抽出に置き換えると安定。
4. **賃貸対応**：現状は売買（GBK001210/GBK002100）のみ。賃貸は GBK001310。種別に応じた分岐。
5. **エラー耐性**：1条件が失敗しても他を継続（現状は open_search で復帰）。ネットワーク/タイムアウトのリトライ強化。
6. **認証情報の保護**：`config.yaml` に平文。環境変数/キーチェーン対応の検討。
7. **物件詳細/概要**：現状は一覧のみ。「詳細」「概要」ボタンから追加情報取得は未実装。

---

## 8. 重要な注意

- REINS は宅建業者向け会員制。**利用規約上、自動アクセスが制限される場合がある**（要確認・自己責任）。
- **認証連続失敗でアカウントがロックされ得る**。デバッグ時は無闇に再実行しない。
- 検索・一覧表示は無料。**図面(PDF)取得は課金**の可能性。
- ログイン後画面は JavaScript 描画。生HTMLフェッチでは中身が取れない（Playwright必須）。
